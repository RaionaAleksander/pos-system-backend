spring:
  datasource:
    password: your_database_password_here   # replace with a real password locally
  security:
    user:
      password: your_admin_password_here    # replace with a real password locally